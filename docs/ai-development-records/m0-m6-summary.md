# M0—M6 AI 辅助开发记录摘要

- 开发范围始终以主规格、确认决策和阶段计划为边界。
- M0—M4 分别完成认证、租户权限、报表导入、分析和优化工作流；M5 完成真实前端闭环及 Chrome E2E；M6 只做索引、幂等检查、部署/验收和性能计划收口。
- AI 生成或修改的代码均通过实际检查、测试或构建验证；未执行的 Docker、真实样例和性能项明确标为 `NOT VERIFIED` 或 `BLOCKED_BY_REAL_SAMPLE`。
- 演示分析固定走 `MockLLMProvider`。未配置真实模型密钥，不保存或展示隐藏思维过程。
- 未读取或复用隔离目录 `amazon-ads-operations-0.1.1`，未把未知 DOCX、`.arts/`、测试产物、密码、Token 或真实数据纳入提交。
- 稳定演示检查点为提交 `3023e84` 和注解标签 `demo-milestone`；M6 不改变其历史。
